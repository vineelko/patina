[package]
name = "sample_components"
version = "2.0.2"
edition = "2021"

[lib]
name = "sample_components"
path = "src/lib.rs"

[dependencies]
log = { workspace = true }
uefi_core = { workspace = true }
uefi_component_interface = { workspace = true }

[features]
std = []
