[package]
name = "patina"
version.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true
readme = "README.md"
description = "Common types and functionality used in UEFI development."

[lints]
workspace = true

[[bench]]
name = "bench_component"
harness = false

[[bench]]
name = "bench_service"
harness = false

[[bench]]
name = "bench_guid"
harness = false

[[example]]
name = "basic_guid_usage"
required-features = ["alloc"]

[[example]]
name = "basic_hob_usage"
required-features = ["alloc"]

[[example]]
name = "brotli"
required-features = ["alloc"]

[[test]]
name = "test_new_component_attr"
required-features = ["alloc"]

[[test]]
name = "compile_fail_tests"
required-features = ["alloc"]

[dependencies]
patina_macro = { workspace = true }
fixedbitset = { workspace = true, optional = true }

bitvec = { workspace = true }
cfg-if = { workspace = true }
compile-time = { workspace = true }
goblin = { workspace = true, features = ["pe32", "pe64", "te", "alloc"], optional = true }
log = { workspace = true }
r-efi = { version = "7.1", default-features = false }
mockall = { workspace = true, optional = true }
num-traits = { workspace = true }
fallible-streaming-iterator = { workspace = true }
safe-mmio = { workspace = true }
scroll = { workspace = true }
uuid = { workspace = true }
serde = { workspace = true, optional = true }
serde_yaml = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
spin = { workspace = true }
zerocopy = { workspace = true }
zerocopy-derive = { workspace = true }

[target.'cfg(target_arch="x86_64")'.dependencies]
uart_16550 = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
mockall = { workspace = true }
serde = { workspace = true }
serde_yaml = { workspace = true }
brotli-decompressor = { workspace = true, default-features = false }
alloc-no-stdlib = { workspace = true }
# trybuild is intentionally only used in patina crate dependencies because
# of unique requirements to test proc macro results. It can be moved to
# the workspace level if a need arises in the future.
trybuild = { version = "1.0", features = ["diff"] }

[features]
core = ['alloc']
std = ['alloc']
alloc = ["dep:goblin", "dep:fixedbitset"]
mockall = ["dep:mockall", "std"]
global_allocator = []
serde = ["alloc", "dep:serde", "serde/alloc", "dep:serde_json"]
serde-with-yaml = ["serde", "dep:serde_yaml"]

unstable = ["unstable-device-path"]
unstable-device-path = []
# All non-std features to test in CI
ci_features = ["core", "alloc", "global_allocator", "unstable", "unstable-device-path"]
